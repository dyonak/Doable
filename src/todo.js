export class Todo {
  constructor(title, createDate, dueDate, status, description) {
    this.title = title;
    this.createDate = createDate;
    this.dueDate = dueDate;
    this.status = status;
    this.description = description;
  }
}
