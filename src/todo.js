export class Todo {
  constructor(title, createDate, dueDate, isComplete, description) {
    this.title = title;
    this.createDate = createDate;
    this.dueDate = dueDate;
    this.isComplete = isComplete;
    this.description = description;
  }
}
